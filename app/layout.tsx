import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "JobMatch - AI-Powered Job Search",
  description: "Upload your CV, extract skills, and find AI-matched job opportunities with compatibility scores",
  keywords: ["job search", "CV parser", "skill matching", "AI", "career"],
  openGraph: {
    title: "JobMatch - Find Your Perfect Job",
    description: "AI-powered job matching based on your skills and experience",
    type: "website",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
